## 🏋️ AI Детектор Отжиманий (Push-up Counter)

**Интеллектуальная система компьютерного зрения для подсчета повторений и контроля техники выполнения отжиманий в реальном времени с использованием веб-камеры.**

---

### ✨ Обзор Проекта

Этот проект представляет собой AI-тренер, который автоматически:
1.  **Обнаруживает позу** с помощью MediaPipe.
2.  **Классифицирует** фазу отжимания (верх/низ) с помощью обученной модели Random Forest.
3.  **Подсчитывает повторения**, используя логику фиксации поз.
4.  Предоставляет **мгновенную обратную связь** о правильности техники (например, контроль коленей).

Архитектура проекта модульная:
* **Frontend (Streamlit):** Управление камерой, отображение UI и реализация логики счета.
* **Backend (FastAPI):** Высокопроизводительный API для классификации, загружающий модель ML.

### 🛠️ Стек Технологий

| Компонент | Технология | Файл(ы) | Назначение |
| :--- | :--- | :--- | :--- |
| **Обнаружение Позы** | `MediaPipe Pose` | `pose_detector.py` | Извлечение 33 ключевых точек суставов в реальном времени. |
| **Классификация Позы** | `RandomForestClassifier` (Scikit-learn) | `train_model.py`, `api.py` | Определение фазы: `pushups_up` (1) / `pushups_down` (0). |
| **Backend API** | `FastAPI` | `api.py` | Асинхронный API для быстрой обработки запросов классификации. |
| **Frontend UI** | `Streamlit` | `app_streamlit.py` | Интерфейс, захват камеры, логика счета и отображение метрик. |
| **Работа с Видео** | `OpenCV (cv2)` | `app_streamlit.py` | Захват и обработка кадров. |

---

### 🚀 Запуск Приложения

Для запуска требуется Python (3.8+) и наличие веб-камеры.

#### 1. Подготовка и Установка Зависимостей

```bash
# Клонируйте репозиторий (если еще не сделали этого)
git clone <URL_ВАШЕГО_РЕПОЗИТОРИЯ>
cd ai-pushup-detector

# Установите все необходимые библиотеки (предполагая наличие requirements.txt)
pip install -r requirements.txt
